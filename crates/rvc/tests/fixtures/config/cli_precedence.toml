# ARCH-4d corpus: representative subset for defaults < file < CLI (ADR-009).

metrics_port = 9090
log_level = "debug"
beacon_url = "http://file-only:5052"
graffiti = "from-file"
keymanager_enabled = true
tracing_sample_rate = 0.25
logfile = "/tmp/from-file.log"
